export const metadata = {
    title: 'Registrace',
    description: 'Vytvořte si účet ve Visualy a začněte tvořit widgety zdarma.',
};

export default function RegistraceLayout({ children }) {
    return children;
}
